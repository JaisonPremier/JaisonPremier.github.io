# Character asset contract

Each fighter folder currently contains:

- `full.png`: transparent full-body artwork used for selection, match, HUD crops, archive, and winner compositions

The shared title composition is `group.png`.

The game deliberately falls back to a typographic combat silhouette when a file is absent. This keeps every flow playable while ensuring no substitute character replaces the supplied art direction.
